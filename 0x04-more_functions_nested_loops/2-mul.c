#include "main.h"

/**
 * mul -multiply two numbers
 * @a: first parameter to be multiplied
 * @b: second paraeter to be multiplied
 * Return: a*b multiple of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
