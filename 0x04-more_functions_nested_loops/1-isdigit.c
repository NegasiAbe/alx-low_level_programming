#include "main.h"

/**
 * _isdigit -checks presence of the digit
 * @c: character argument
 * Return: 1 if the digit is present or 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
