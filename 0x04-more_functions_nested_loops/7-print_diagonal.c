#include "main.h"

/**
 * print_diagonal -prints a backslash in diagonal
 */
void print_diagonal(void)
{
	int i, j;

	if (i <= 0)
		_putchar('\n');
	else
	{
		for (j = 0; j < i; j++)
			for (j = 0; j < i; j++)

			{
				_putchar(' ');
			_putchar(92);
			_putchar('\n');
			}
	}
}
