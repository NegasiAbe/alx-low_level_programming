#include<stdio.h>

/**
 *  main -Prints alphabet in lower case, then in upper case
 *
 *  Return 0: sucess 
 */
int main(void)
{
	char c = 'a';
	char C = 'A';

	while (c <= z)
	{
		putchar(c);
		c++;
	}
	while(C <= Z)
	{
		putchar(C);
		C++;
	}
	putchar('\n');
	return (0);
}
